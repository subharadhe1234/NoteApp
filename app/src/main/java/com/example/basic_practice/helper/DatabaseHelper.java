package com.example.basic_practice.helper;import android.content.Context;import android.database.sqlite.SQLiteDatabase;import android.database.sqlite.SQLiteOpenHelper;import android.util.Log;public class DatabaseHelper extends SQLiteOpenHelper {    //  database name for the data base    private static final String DATABASE_NAME = "messaage.db";    //    table name for the database    private static final String TABLE_NAME = "note";    //    database version for the data base    private static final int DATABASE_VERSION = 1;    //    Table colum for the data base    private static final String ID = "id";    private static final String TITLE = "title";    private static final String BODY = "body";    /*     * creat table for the data base     * and add column for the data base     * */    private static final String CREAT_TABLE = "CREATE TABLE " + TABLE_NAME + " (" + ID +            " INTEGER PRIMARY KEY AUTOINCREMENT, " + TITLE + " TEXT, " + BODY + " TEXT);";    public DatabaseHelper(Context context) {        super(context, DATABASE_NAME, null, DATABASE_VERSION);    }    @Override    public void onCreate(SQLiteDatabase db) {        /*         * create a tabel for the data base         * */        Log.d("aaaaaaaaaaa", "onCreate1: " + db + " " + CREAT_TABLE);        db.execSQL(CREAT_TABLE);        Log.d("aaaaaaaaaaa", "onCreate: " + db);    }    @Override    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {        /*         * drop table if exist         * and create a new table for the same database         * */        db.execSQL("DROP TABLE IF EXISTS " + TABLE_NAME);        onCreate(db);    }}